/* ---------------------------------------------------*
 |	tconfig.h.in
 |	Default tconfig.h for bld-port.
 |	-- must be kept identical with tconfig.in 
 |		(DOS can't) handle a.b.c filenames
 *-----------------------------------------------------*/
#include "dfltsys.h" 
