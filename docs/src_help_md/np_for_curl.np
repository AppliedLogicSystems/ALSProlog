np(
   core_prolog,  %Package
   input_output,  %Group
   'curl/[1,2,3]',  %Title
   curl123,  %FileName
   % Preds With Descriptions:
   [p('curl/1', 'Combined URL, Options, and Target all in one list'),
    p('curl/2', 'Separate URL arg, combined Options, and Target in one list'),
    p('curl/3', 'Separate URL, Options, and Target args'),
    p('http/3', 'REST-inspired user-level interface for curl')
   ]
  ).
